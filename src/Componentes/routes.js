import { BrowserRouter as Router, Route, Switch } from 'react-router-dom';

function rota() {
  return (
    <Router>
      <Switch>
        {'/contato'}
      </Switch>
    </Router>
  );
}

export default rota;
